import React from 'react'
import { render } from 'enzyme'

const ContactList = (props) => { 
  const allContacts = props.contacts.map((contact, index) => {

  })
  return (
    <div class="contact-list">
      
    </div>
  )
}

export default ContactList
